to use 
https://github.com/so-fancy/diff-so-fancy

to use vundle
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

install zsh
to do symlinks to dotfiles
colors for vim
backup folders for vim

install go

1. to create a script that listens for display changes and reruns the monitors/backgrounds script to;
- set displays (external or only internal) (give the posibility of clone?)
- set backgrounds right
- move workspaces where they belong

2. scripts to dotfiles repo
